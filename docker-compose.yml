services:
  api:
    build: .
    image: transcribe-cloud:local
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./storage:/app/storage
    restart: unless-stopped
