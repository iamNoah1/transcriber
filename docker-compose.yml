services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - tc_data:/app/data
      - tc_storage:/app/storage
    restart: unless-stopped

volumes:
  tc_data:
  tc_storage:
