services:
  api:
    build: .
    ports:
      - "3030:3030"
    env_file: .env
    volumes:
      - tc_data:/app/data
      - tc_storage:/app/storage
    restart: unless-stopped

volumes:
  tc_data:
  tc_storage:
